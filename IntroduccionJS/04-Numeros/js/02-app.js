//Objeto Math
let resultado;


resultado = Math.PI; // Valor de PI
resultado = Math.round(2.5); // Redondea al entero más cercano
resultado = Math.ceil(2.1); // Redondea hacia arriba
resultado = Math.floor(2.9); // Redondea hacia abajo
resultado = Math.sqrt(144); // Raíz cuadrada
resultado = Math.abs(-200); // Valor absoluto
resultado = Math.min(3, 5, 1, 8, 2, 10); // Valor mínimo
resultado = Math.max(3, 5, 1, 8, 2, 10); // Valor máximo
resultado = Math.random(); // Número aleatorio entre 0 y 1
resultado = Math.random() * 30; // Número aleatorio entre 0 y 30
resultado = Math.floor(Math.random() * 30); // Número aleatorio entre 0 y 29


console.log(resultado);
